# Exemplo de configuração do .env para AppEmpreiteiros
# Copie este arquivo para .env e preencha os valores

# Configurações do Node
NODE_ENV=development  # Use 'production' no Render
PORT=3000

# Segurança
SESSION_SECRET=sua_senha_secreta_aqui  # Gere uma string forte e aleatória

# Método de acesso ao banco de dados - recomendamos usar a API para maior compatibilidade
USE_SUPABASE_API=true

# Credenciais do Supabase (necessárias quando USE_SUPABASE_API=true)
SUPABASE_URL=https://swrnbxuvewboetodewbi.supabase.co
SUPABASE_KEY=sua_chave_anon_key_do_supabase  # Encontre em: Configurações do Projeto > API > anon/public key

# Forçar SQLite (use apenas para depuração ou se tiver problemas com Supabase)
# FORCE_SQLITE=true

# Antiga URL de conexão direta ao PostgreSQL (necessária apenas se USE_SUPABASE_API=false)
# DATABASE_URL=postgres://postgres:senha_do_postgres@db.swrnbxuvewboetodewbi.supabase.co:5432/postgres

# Outras configurações opcionais
# OPENAI_API_KEY=sua_chave_openai  # Necessária apenas se usar recursos de IA