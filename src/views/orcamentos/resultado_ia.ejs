<div class="row justify-content-center">
  <div class="col-md-10">
    <div class="card shadow-sm">
      <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
        <h4 class="mb-0">
          <i class="fas fa-robot me-2"></i>
          Orçamento Gerado por IA
        </h4>
      </div>
      <div class="card-body">
        <div class="alert alert-info">
          <i class="fas fa-info-circle me-2"></i>
          Este é um orçamento gerado automaticamente com base nos parâmetros fornecidos. Revise os valores e detalhes antes de salvar.
        </div>
        
        <div class="row mb-4">
          <div class="col-md-6">
            <h5 class="border-bottom pb-2">Parâmetros Utilizados</h5>
            <p><strong>Tipo de Obra:</strong> <%= dados.tipo_obra %></p>
            <p><strong>Localidade:</strong> <%= dados.localidade %></p>
            <p><strong>Tamanho/Escopo:</strong> <%= dados.tamanho %></p>
            <% if (dados.descricao) { %>
              <p><strong>Descrição:</strong> <%= dados.descricao %></p>
            <% } %>
          </div>
        </div>

        <div class="row">
          <div class="col-12">
            <h5 class="border-bottom pb-2">Orçamento Detalhado</h5>
            <div class="p-3 bg-light rounded">
              <pre class="mb-0" style="white-space: pre-wrap;"><%= orcamento %></pre>
            </div>
          </div>
        </div>

        <div class="mt-4">
          <form action="/orcamentos/salvar-orcamento-ia" method="POST">
            <input type="hidden" name="cliente_id" value="<%= cliente_id %>">
            <input type="hidden" name="tipo_obra" value="<%= dados.tipo_obra %>">
            <input type="hidden" name="localidade" value="<%= dados.localidade %>">
            
            <div class="mb-3">
              <label for="titulo" class="form-label">Título do Orçamento</label>
              <input type="text" class="form-control" id="titulo" name="titulo" required 
                value="Orçamento <%= dados.tipo_obra %> - <%= dados.localidade %>">
            </div>
            
            <div class="mb-3">
              <label for="descricao" class="form-label">Descrição</label>
              <textarea class="form-control" id="descricao" name="descricao" rows="3" required><%= orcamento %></textarea>
            </div>
            
            <div class="mb-3">
              <label for="valor_total" class="form-label">Valor Total</label>
              <input type="number" step="0.01" class="form-control" id="valor_total" name="valor_total" required>
              <small class="text-muted">Insira o valor total extraído do orçamento gerado.</small>
            </div>
            
            <div class="d-flex justify-content-between">
              <button type="submit" class="btn btn-success">
                <i class="fas fa-save me-2"></i>Salvar Orçamento
              </button>
              <div>
                <a href="/orcamentos/gerar-ia" class="btn btn-secondary me-2">Gerar Novo</a>
                <a href="/orcamentos" class="btn btn-outline-secondary">Cancelar</a>
              </div>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>